DATABASE_URL="file:./dev.db"

# 以下2つを設定するとパスワード認証が有効になる(未設定の場合は認証なしで動作する)。
# ローカルのみで使う分には不要だが、自宅サーバー等インターネットからアクセス
# 可能な場所にホスティングする場合は設定を強く推奨する。
# AUTH_PASSWORD="your_password"
# SESSION_SECRET は `openssl rand -base64 32` 等で生成した値を設定する
# AUTH_PASSWORD を設定する場合は必須。
# SESSION_SECRET="your_secret_key"
